const num1 = 5
const num2 = 10

function addValues() {
  console.log(`the sum is : ${num1 + num2}`)
}

addValues()
//Without exporting it works in another file (or module)
//Note - Directory is equivalent to folder